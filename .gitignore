node_modules/
dist/
plugins/*/dist/
plugins/*/node_modules/
.cache/
*.log
trace-generator/bin
.worktrees/
__pycache__/
*.pyc
.eslintcache
# Superpowers planning artifacts stay on disk, never in git
docs/superpowers/
.superpowers/

# macOS
.DS_Store

# make up writes a live Grafana service-account token in here every run.
# bootstrap-grafana-token creates it from scratch, so nothing needs a template.
docker/.env.slo
